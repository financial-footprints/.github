meta {
  name: recovery advanced begin
  type: http
  seq: 53
}

post {
  url: {{jwtUrl}}/api/v1/auth/recovery/advanced/begin
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "alice",
    "email": "alice@example.com"
  }
}

tests {
  test("always 200", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.message).to.include("if an account exists");
  });
}
