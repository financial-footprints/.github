meta {
  name: webauthn register begin unauthorized
  type: http
  seq: 47
}

post {
  url: {{jwtUrl}}/api/v1/auth/mfa/webauthn/register/begin
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

tests {
  test("401 unauthorized", function () {
    expect(res.getStatus()).to.equal(401);
    expect(res.getBody().details).to.equal("unauthorized");
  });
}
