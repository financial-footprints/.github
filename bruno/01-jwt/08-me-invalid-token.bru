meta {
  name: me invalid bearer
  type: http
  seq: 8
}

get {
  url: {{jwtUrl}}/api/v1/auth/me
}

headers {
  Authorization: Bearer not-a-jwt
}

tests {
  test("401 unauthorized", function () {
    expect(res.getStatus()).to.equal(401);
    expect(res.getBody().details).to.equal("unauthorized");
  });
}
