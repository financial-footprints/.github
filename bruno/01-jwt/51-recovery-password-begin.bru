meta {
  name: recovery password begin
  type: http
  seq: 51
}

post {
  url: {{jwtUrl}}/api/v1/auth/recovery/password/begin
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "alice",
    "email": "alice@example.com"
  }
}

tests {
  test("always 200", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.message).to.include("if an account exists");
  });
}
