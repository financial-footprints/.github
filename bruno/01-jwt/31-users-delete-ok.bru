meta {
  name: delete user
  type: http
  seq: 31
}

delete {
  url: {{jwtUrl}}/api/v1/auth/users/{{doomedId}}
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

tests {
  test("deleted", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data).to.equal(null);
  });
}
