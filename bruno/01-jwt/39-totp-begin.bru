meta {
  name: totp begin as alice
  type: http
  seq: 39
}

post {
  url: {{jwtUrl}}/api/v1/auth/mfa/totp/begin
  body: json
}

headers {
  Authorization: Bearer {{aliceAccessToken}}
  Content-Type: application/json
}

body:json {
  {
    "password": "password123"
  }
}

tests {
  test("totp uri", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.uri).to.include("otpauth://totp/");
  });
}
