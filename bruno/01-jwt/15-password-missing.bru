meta {
  name: password missing fields
  type: http
  seq: 15
}

post {
  url: {{jwtUrl}}/api/v1/auth/password
  body: json
}

headers {
  Authorization: Bearer {{aliceAccessToken}}
  Content-Type: application/json
}

body:json {
  {
    "current_password": "password123"
  }
}

tests {
  test("400 required", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().details).to.equal("current_password and new_password required");
  });
}
