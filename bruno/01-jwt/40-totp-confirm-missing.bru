meta {
  name: totp confirm missing code
  type: http
  seq: 40
}

post {
  url: {{jwtUrl}}/api/v1/auth/mfa/totp/confirm
  body: json
}

headers {
  Authorization: Bearer {{aliceAccessToken}}
  Content-Type: application/json
}

body:json {
  {}
}

tests {
  test("400 code required", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().details).to.equal("code required");
  });
}
