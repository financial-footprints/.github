meta {
  name: login begin missing fields
  type: http
  seq: 2
}

post {
  url: {{jwtUrl}}/api/v1/auth/login/begin
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "alice"
  }
}

tests {
  test("400 required fields", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().details).to.equal("challenge_id, username, and client_public required");
  });
}
