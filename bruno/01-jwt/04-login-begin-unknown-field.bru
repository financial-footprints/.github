meta {
  name: login begin unknown json field
  type: http
  seq: 4
}

post {
  url: {{jwtUrl}}/api/v1/auth/login/begin
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "challenge_id": "00000000-0000-4000-8000-000000000000",
    "username": "alice",
    "client_public": "AQ",
    "extra": true
  }
}

tests {
  test("400 invalid json", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().details).to.equal("invalid json");
  });
}
