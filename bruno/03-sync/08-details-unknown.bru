meta {
  name: account details unknown
  type: http
  seq: 8
}

get {
  url: {{syncUrl}}/api/v1/sync/accounts/details?account_id={{unknownUuid}}
}

headers {
  Authorization: Bearer {{aliceAccessToken}}
  X-User-Id: {{userId}}
  X-Vault-Verification: {{vaultVerification}}
}

tests {
  test("404", function () {
    expect(res.getStatus()).to.equal(404);
  });
}
