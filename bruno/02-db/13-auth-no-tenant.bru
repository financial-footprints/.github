meta {
  name: missing tenant headers
  type: http
  seq: 13
}

get {
  url: {{dbUrl}}/api/v1/accounts
}

headers {
  Authorization: Bearer {{aliceAccessToken}}
}

tests {
  test("401 missing tenant headers", function () {
    expect(res.getStatus()).to.equal(401);
    expect(res.getBody().details).to.equal("missing tenant headers");
  });
}
